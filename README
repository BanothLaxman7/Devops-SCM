This is testing

##### GIT REMOTE COMMANDS ######


git remote -v ===> for checking remote refer
git remote add origin <URL> 
git remote rm origin 

git push origin <branch_name>

git push origin --all (pushing all branches at atime)

